# MORSE_CODE
Arduino-based Morse Code generator that converts text (A–Z, 0–9) into standard Morse signals. The message is entered via Serial Monitor and transmitted using an LED and buzzer with proper timing control, demonstrating digital I/O interfacing and embedded programming.
